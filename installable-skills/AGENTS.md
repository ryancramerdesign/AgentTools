# Installable Skills

This directory contains distributable skill files that AgentTools can install
into a ProcessWire project root for AI coding agents to consume.

When working in the AgentTools module source checkout, follow the root
`AGENTS.md` file first. Files under `installable-skills/` are package contents,
not the primary instructions for editing this module.
